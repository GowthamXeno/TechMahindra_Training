import { FilterteamsizePipe } from './filterteamsize.pipe';

describe('FilterteamsizePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterteamsizePipe();
    expect(pipe).toBeTruthy();
  });
});
